name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Release version (e.g. 1.2.0)'
        required: true
        type: string
      prerelease:
        description: 'Mark as pre-release'
        required: false
        type: boolean
        default: false
      deploy_cloudflare:
        description: 'Deploy Web to Cloudflare Workers'
        required: false
        type: boolean
        default: true
      notes:
        description: 'Release notes (markdown, optional)'
        required: false
        type: string
        default: ''

env:
  FLUTTER_VERSION: 3.41.6
  APP_REPO_OWNER: ${{ github.repository_owner }}
  APP_REPO_NAME: yetanotherbusapp
  APP_WORKFLOW_API_ID: build.yml
  APP_WORKFLOW_NIGHTLY_ID: build
  APP_UPDATE_CHANNEL: release
  IOS_FLAVOR: YABus
  YABUS_GOOGLE_WEB_CLIENT_ID: ${{ secrets.YABUS_GOOGLE_WEB_CLIENT_ID }}
  YABUS_GOOGLE_IOS_CLIENT_ID: ${{ secrets.YABUS_GOOGLE_IOS_CLIENT_ID }}

permissions:
  contents: write

jobs:
  # ── Verify & build ──────────────────────────────────────────────
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Prepare Android signing files
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_ALIAS: ${{ secrets.KEYSTORE_ALIAS }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        run: |
          if [[ -n "$KEYSTORE_BASE64" && -n "$KEYSTORE_ALIAS" && -n "$KEYSTORE_PASSWORD" ]]; then
            echo "$KEYSTORE_BASE64" | base64 --decode > android/app/upload-keystore.jks
            printf '%s\n' \
              "storePassword=$KEYSTORE_PASSWORD" \
              "keyPassword=$KEYSTORE_PASSWORD" \
              "keyAlias=$KEYSTORE_ALIAS" \
              "storeFile=upload-keystore.jks" \
              > android/key.properties
          else
            echo "Signing secrets are missing; skip generating release keystore files."
          fi
      - name: Bump version
        shell: bash
        run: |
          sed -i "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Build APK
        run: >
          flutter build apk --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=android-apk-release
      - name: Rename APK
        shell: bash
        run: |
          mv build/app/outputs/flutter-apk/app-release.apk \
             build/app/outputs/flutter-apk/YABus-${{ inputs.version }}.apk
      - name: Upload Android artifact
        uses: actions/upload-artifact@v6
        with:
          name: android-apk-release
          path: build/app/outputs/flutter-apk/YABus-${{ inputs.version }}.apk

      - name: Build AppBundle
        env:
          APP_BUILD_AAB: "true"
        run: >
          flutter build appbundle --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=android-aab-release
          --dart-define=APP_BUILD_AAB=true
      - name: Rename AppBundle
        shell: bash
        run: |
          mv build/app/outputs/bundle/release/app-release.aab \
             build/app/outputs/bundle/release/YABus-${{ inputs.version }}.aab
      - name: Upload Android AppBundle artifact
        uses: actions/upload-artifact@v6
        with:
          name: android-aab-release
          path: build/app/outputs/bundle/release/YABus-${{ inputs.version }}.aab

  build-wearos:
    name: Build Wear OS APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Prepare Wear OS signing files
        shell: bash
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
          KEYSTORE_ALIAS: ${{ secrets.KEYSTORE_ALIAS }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        run: |
          if [[ -n "$KEYSTORE_BASE64" && -n "$KEYSTORE_ALIAS" && -n "$KEYSTORE_PASSWORD" ]]; then
            echo "$KEYSTORE_BASE64" | base64 --decode > android/wearos/upload-keystore.jks
            printf '%s\n' \
              "storePassword=$KEYSTORE_PASSWORD" \
              "keyPassword=$KEYSTORE_PASSWORD" \
              "keyAlias=$KEYSTORE_ALIAS" \
              "storeFile=upload-keystore.jks" \
              > android/wearos/key.properties
          else
            echo "Signing secrets are missing; skip generating release keystore files."
          fi
      - name: Bump version
        shell: bash
        run: |
          sed -i "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Generate Gradle files
        run: flutter build apk --config-only
      - name: Create local.properties for Wear OS
        shell: bash
        run: |
          FLUTTER_BIN=$(which flutter)
          FLUTTER_SDK=$(dirname $(dirname "$FLUTTER_BIN"))
          echo "flutter.sdk=$FLUTTER_SDK" > android/wearos/local.properties
          cat android/wearos/local.properties
      - name: Build Wear OS APK and AAB
        env:
          APP_GIT_SHA: ${{ github.sha }}
        run: |
          cd android
          ./gradlew :wearos:assembleRelease :wearos:bundleRelease
      - name: Rename Wear OS Artifacts
        shell: bash
        run: |
          mv build/wearos/outputs/apk/release/wearos-release.apk \
             build/wearos/outputs/apk/release/YABus-WearOS-${{ inputs.version }}.apk
          mv build/wearos/outputs/bundle/release/wearos-release.aab \
             build/wearos/outputs/bundle/release/YABus-WearOS-${{ inputs.version }}.aab
      - name: Upload Wear OS APK artifact
        uses: actions/upload-artifact@v6
        with:
          name: wearos-apk-release
          path: build/wearos/outputs/apk/release/YABus-WearOS-${{ inputs.version }}.apk
      - name: Upload Wear OS AppBundle artifact
        uses: actions/upload-artifact@v6
        with:
          name: wearos-aab-release
          path: build/wearos/outputs/bundle/release/YABus-WearOS-${{ inputs.version }}.aab

  build-web:
    name: Build Web
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Bump version
        shell: bash
        run: |
          sed -i "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Build Web
        run: >
          flutter build web --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=web-release
      - name: Stamp web build (sw.js cache version + version.json)
        shell: bash
        env:
          APP_VERSION: ${{ inputs.version }}
          APP_BUILD_NUMBER: ${{ github.run_number }}
          APP_GIT_SHA: ${{ github.sha }}
        run: node scripts/stamp-web-build.mjs
      - name: Upload Web artifact
        uses: actions/upload-artifact@v6
        with:
          name: web-release
          path: build/web

  build-windows:
    name: Build Windows
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Enable Windows desktop
        run: flutter config --enable-windows-desktop
      - name: Install dependencies
        run: flutter pub get
      - name: Bump version
        shell: pwsh
        run: |
          $updated = (Get-Content pubspec.yaml) -replace '^version:.*$', 'version: ${{ inputs.version }}+${{ github.run_number }}'
          $updated | Set-Content pubspec.yaml -Encoding utf8NoBOM
          Select-String '^version:' pubspec.yaml
      - name: Build Windows
        run: >
          flutter build windows --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=windows-release
      - name: Install NSIS
        shell: pwsh
        run: |
          choco install nsis -y
          $nsisExe = Join-Path ${env:ProgramFiles(x86)} 'NSIS\makensis.exe'
          if (-not (Test-Path $nsisExe)) {
            throw "makensis.exe not found at $nsisExe"
          }
          & $nsisExe /VERSION
      - name: Build NSIS installer
        shell: pwsh
        run: |
          $nsisExe = Join-Path ${env:ProgramFiles(x86)} 'NSIS\makensis.exe'
          $sourceDir = Join-Path $env:GITHUB_WORKSPACE 'build\windows\x64\runner\Release'
          if (-not (Test-Path $nsisExe)) {
            throw "makensis.exe not found at $nsisExe"
          }
          if (-not (Test-Path $sourceDir)) {
            throw "Windows build output not found at $sourceDir"
          }
          & $nsisExe `
            "/DVERSION=${{ inputs.version }}" `
            "/DSOURCE_DIR=$sourceDir" `
            windows\installer\yabus.nsi
      - name: Upload Windows installer artifact
        uses: actions/upload-artifact@v6
        with:
          name: windows-release
          path: windows/installer/YABus-${{ inputs.version }}-windows-x64-setup.exe

  build-linux:
    name: Build Linux
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Linux dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang cmake ninja-build pkg-config libgtk-3-dev libnotify-dev dpkg-dev
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Enable Linux desktop
        run: flutter config --enable-linux-desktop
      - name: Install dependencies
        run: flutter pub get
      - name: Bump version
        shell: bash
        run: |
          sed -i "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Build Linux
        run: >
          flutter build linux --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=linux-release
      - name: Package Linux .deb and AppImage
        shell: bash
        run: |
          chmod +x linux/installer/create_linux_packages.sh
          bash linux/installer/create_linux_packages.sh \
            build/linux/x64/release/bundle \
            ${{ inputs.version }} \
            build/linux/output
      - name: Upload Linux .deb artifact
        uses: actions/upload-artifact@v6
        with:
          name: linux-deb-release
          path: build/linux/output/*.deb
      - name: Upload Linux AppImage artifact
        uses: actions/upload-artifact@v6
        with:
          name: linux-appimage-release
          path: build/linux/output/*.AppImage

  build-macos:
    name: Build macOS
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Enable macOS desktop
        run: flutter config --enable-macos-desktop
      - name: Install dependencies
        run: flutter pub get
      - name: Bump version
        shell: bash
        run: |
          sed -i '' "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Build macOS
        run: >
          flutter build macos --release
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=macos-release
      - name: Package macOS DMG
        shell: bash
        run: |
          APP_PATH=$(find build/macos/Build/Products/Release -maxdepth 1 -name "*.app" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "No macOS .app bundle found."
            exit 1
          fi
          mkdir -p build/macos/output
          chmod +x macos/installer/create_dmg.sh
          bash macos/installer/create_dmg.sh "$APP_PATH" ${{ inputs.version }} build/macos/output
      - name: Upload macOS artifact
        uses: actions/upload-artifact@v6
        with:
          name: macos-release
          path: build/macos/output/YABus-${{ inputs.version }}-macos.dmg

  build-ios-ipa:
    name: Build iOS unsigned IPA
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true
      - name: Install dependencies
        run: flutter pub get
      - name: Bump version
        shell: bash
        run: |
          sed -i '' "s/^version:.*/version: ${{ inputs.version }}+${{ github.run_number }}/" pubspec.yaml
          grep '^version:' pubspec.yaml
      - name: Prepare iOS build
        run: flutter precache --ios
      # Generate Flutter settings and install pods; let xcodebuild report build errors directly.
      - name: Configure iOS build without codesign
        run: >
          flutter build ios --release --no-codesign --config-only
          --flavor=${{ env.IOS_FLAVOR }}
          --dart-define=APP_GIT_SHA=${{ github.sha }}
          --dart-define=APP_UPDATE_CHANNEL=${{ env.APP_UPDATE_CHANNEL }}
          --dart-define=APP_REPO_OWNER=${{ env.APP_REPO_OWNER }}
          --dart-define=APP_REPO_NAME=${{ env.APP_REPO_NAME }}
          --dart-define=APP_WORKFLOW_API_ID=${{ env.APP_WORKFLOW_API_ID }}
          --dart-define=APP_WORKFLOW_NIGHTLY_ID=${{ env.APP_WORKFLOW_NIGHTLY_ID }}
          --dart-define=YABUS_GOOGLE_WEB_CLIENT_ID=${{ env.YABUS_GOOGLE_WEB_CLIENT_ID }}
          --dart-define=YABUS_GOOGLE_IOS_CLIENT_ID=${{ env.YABUS_GOOGLE_IOS_CLIENT_ID }}
          --dart-define=APP_NIGHTLY_ARTIFACT=ios-unsigned-ipa
      - name: Archive YABus.app
        env:
          APP_GIT_SHA: ${{ github.sha }}
        run: |
          xcodebuild \
            -hideShellScriptEnvironment \
            -workspace ios/YABus.xcworkspace \
            -scheme ${{ env.IOS_FLAVOR }} \
            -configuration Release-${{ env.IOS_FLAVOR }} \
            -sdk iphoneos \
            -destination generic/platform=iOS \
            -archivePath build/ipa/YABus.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            archive
      - name: Package unsigned IPA
        shell: bash
        run: |
          mkdir -p build/ipa/temp/Payload
          cp -R build/ipa/YABus.xcarchive/Products/Applications/*.app build/ipa/temp/Payload/
          cd build/ipa/temp
          zip -r Generated_IPA.zip Payload
          mv Generated_IPA.zip ../YABus-${{ inputs.version }}.ipa
          cd ../../..
          rm -rf build/ipa/temp
      - name: Upload iOS artifact
        uses: actions/upload-artifact@v6
        with:
          name: ios-unsigned-ipa
          path: build/ipa/YABus-${{ inputs.version }}.ipa

  # ── GitHub Pages ────────────────────────────────────────────────
  deploy-cloudflare:
    name: Deploy to Cloudflare Workers
    needs: build-web
    if: inputs.deploy_cloudflare == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Download Web artifact
        uses: actions/download-artifact@v5
        with:
          name: web-release
          path: build/web
      - name: Deploy Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: deploy

  # ── Create GitHub Release ──────────────────────────────────────
  create-release:
    name: Create GitHub Release
    needs:
      - build-android
      - build-wearos
      - build-web
      - build-windows
      - build-linux
      - build-macos
      - build-ios-ipa
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Download Android APK
        uses: actions/download-artifact@v5
        with:
          name: android-apk-release
          path: release-assets
      - name: Download Android AppBundle
        uses: actions/download-artifact@v5
        with:
          name: android-aab-release
          path: release-assets
      - name: Download Wear OS APK
        uses: actions/download-artifact@v5
        with:
          name: wearos-apk-release
          path: release-assets
      - name: Download Wear OS AppBundle
        uses: actions/download-artifact@v5
        with:
          name: wearos-aab-release
          path: release-assets
      - name: Download iOS IPA
        uses: actions/download-artifact@v5
        with:
          name: ios-unsigned-ipa
          path: release-assets
      - name: Download Windows installer
        uses: actions/download-artifact@v5
        with:
          name: windows-release
          path: release-assets
      - name: Download Linux .deb
        uses: actions/download-artifact@v5
        with:
          name: linux-deb-release
          path: release-assets
      - name: Download Linux AppImage
        uses: actions/download-artifact@v5
        with:
          name: linux-appimage-release
          path: release-assets
      - name: Download macOS DMG
        uses: actions/download-artifact@v5
        with:
          name: macos-release
          path: release-assets
      - name: Download Web bundle
        uses: actions/download-artifact@v5
        with:
          name: web-release
          path: release-assets/web
      - name: Package Web artifact
        shell: bash
        run: |
          cd release-assets/web
          zip -r ../YABus-${{ inputs.version }}-web.zip .
      - name: List release assets
        run: find release-assets -maxdepth 2 -type f -exec ls -lh {} \;
      - name: Build release notes
        id: notes
        shell: bash
        run: |
          RELEASE_DOWNLOAD_BASE="https://github.com/${{ github.repository }}/releases/download/${{ inputs.version }}"
          DOWNLOAD_TABLE=$(cat <<EOF
          <!-- YABUS_RELEASE_DOWNLOAD_TABLE_START -->

          ## 下載

          | 平台 | 包裝 | 下載 |
          | --- | --- | --- |
          | Android | APK | [YABus-${{ inputs.version }}.apk]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}.apk) |
          | Android | AAB | [YABus-${{ inputs.version }}.aab]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}.aab) |
          | Wear OS | APK | [YABus-WearOS-${{ inputs.version }}.apk]($RELEASE_DOWNLOAD_BASE/YABus-WearOS-${{ inputs.version }}.apk) |
          | Wear OS | AAB | [YABus-WearOS-${{ inputs.version }}.aab]($RELEASE_DOWNLOAD_BASE/YABus-WearOS-${{ inputs.version }}.aab) |
          | iOS | Unsigned IPA | [YABus-${{ inputs.version }}.ipa]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}.ipa) |
          | Windows | Installer | [YABus-${{ inputs.version }}-windows-x64-setup.exe]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}-windows-x64-setup.exe) |
          | Linux | DEB | [YABus-${{ inputs.version }}-linux-amd64.deb]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}-linux-amd64.deb) |
          | Linux | AppImage | [YABus-${{ inputs.version }}-linux-x86_64.AppImage]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}-linux-x86_64.AppImage) |
          | macOS | DMG | [YABus-${{ inputs.version }}-macos.dmg]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}-macos.dmg) |
          | Web | ZIP | [YABus-${{ inputs.version }}-web.zip]($RELEASE_DOWNLOAD_BASE/YABus-${{ inputs.version }}-web.zip) |

          <!-- YABUS_RELEASE_DOWNLOAD_TABLE_END -->
          EOF
          )
          if [[ -n "${{ inputs.notes }}" ]]; then
            NOTES="${{ inputs.notes }}"
          else
            NOTES="## YABus ${{ inputs.version }}"
            NOTES+=$'\n'"Release build from commit \`${{ github.sha }}\`."
          fi
          NOTES="${NOTES%"${NOTES##*[![:space:]]}"}"
          if [[ -n "$NOTES" ]]; then
            NOTES+=$'\n\n'
          fi
          NOTES+="$DOWNLOAD_TABLE"
          echo "body<<EOF" >> "$GITHUB_OUTPUT"
          echo "$NOTES" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"
      - name: Create Draft Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.version }}
          name: YABus ${{ inputs.version }}
          body: ${{ steps.notes.outputs.body }}
          draft: true
          prerelease: ${{ inputs.prerelease }}
          files: |
            release-assets/YABus-${{ inputs.version }}.apk
            release-assets/YABus-${{ inputs.version }}.aab
            release-assets/YABus-WearOS-${{ inputs.version }}.apk
            release-assets/YABus-WearOS-${{ inputs.version }}.aab
            release-assets/YABus-${{ inputs.version }}.ipa
            release-assets/YABus-${{ inputs.version }}-windows-x64-setup.exe
            release-assets/YABus-${{ inputs.version }}-linux-amd64.deb
            release-assets/YABus-${{ inputs.version }}-linux-x86_64.AppImage
            release-assets/YABus-${{ inputs.version }}-macos.dmg
            release-assets/YABus-${{ inputs.version }}-web.zip
